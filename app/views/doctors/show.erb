<h1><%= @doctor.name %></h1>
<h4>Department: <%= @doctor.department %></h4><br>
<h4>Appointments:</h4>
<ul>
  <% @doctor.appointments.each do |appointment| %>
    <%= appointment.appointment_datetime.strftime("%B %e, %Y at %H:%M") %>
    <%= link_to appointment.patient.name, patient_path(appointment.patient) %></br>
  <% end %>
</ul>
